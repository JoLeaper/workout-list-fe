describe('characters reducers', () => {
  it('handles the SET_WORKOUTS action', () => {
    const state = {
      workouts: [],
      newWorkout: {}
    };
  });

  it('handles the ADD_NEW_WORKOUT action', () => {
    
  });
});
